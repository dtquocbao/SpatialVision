# Local
VITE_API_URL=http://localhost:8000

# Production (Vercel) — set in Vercel Project → Settings → Environment Variables
# Use the direct Space host (not huggingface.co/spaces/...):
# VITE_API_URL=https://dtquocbao-spatialvision-api.hf.space
